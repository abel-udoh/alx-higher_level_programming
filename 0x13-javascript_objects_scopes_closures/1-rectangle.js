#!/usr/bin/node
// a class Rectangle that defines a rectangle
// the constructor takes in two arguments width and height

class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
